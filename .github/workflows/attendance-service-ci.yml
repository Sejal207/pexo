name: Attendance Service CI

on:
  push:
    paths:
      - 'services/attendance-service/**'
      - 'shared/python-libs/**'
      - '.github/workflows/attendance-service-ci.yml'
  pull_request:
    paths:
      - 'services/attendance-service/**'
      - 'shared/python-libs/**'
      - '.github/workflows/attendance-service-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff pytest pytest-asyncio httpx
          if [ -f services/attendance-service/requirements.txt ]; then pip install -r services/attendance-service/requirements.txt; fi

      - name: Lint with Ruff
        run: |
          ruff check services/attendance-service

      - name: Run Tests
        run: |
          pytest services/attendance-service/tests
