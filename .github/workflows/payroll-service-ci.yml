name: Payroll Service CI

on:
  push:
    paths:
      - 'services/payroll-service/**'
      - 'shared/python-libs/**'
      - '.github/workflows/payroll-service-ci.yml'
  pull_request:
    paths:
      - 'services/payroll-service/**'
      - 'shared/python-libs/**'
      - '.github/workflows/payroll-service-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff pytest pytest-asyncio httpx
          if [ -f services/payroll-service/requirements.txt ]; then pip install -r services/payroll-service/requirements.txt; fi

      - name: Lint with Ruff
        run: |
          ruff check services/payroll-service

      - name: Run Tests
        run: |
          pytest services/payroll-service/tests
